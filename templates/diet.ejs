<!DOCTYPE html>
<html lang="fa" dir="rtl">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= body.title %></title>
  <link rel="stylesheet" href="../diet-html/assets/css/output.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

  <body class="bg-white">
    <% if(body.couchName || body.title || body.couchLogo) { %>
    <header class="bg-primary px-4 py-1">
      <div class="flex items-center justify-between">
        <div class="text-white">
          <h2 class="text-2xl mb-2 font-bold"><%= body.couchName %></h2>
          <h1 class="text-xl"><%= body.title %></h1>
        </div>
        <% if(typeof body.couchLogo != "undefined") { %>
        <img class="w-40" src="<%= body.couchLogo %>" alt="logo">
        <% } %>
      </div>
    </header>
    <% } %>

    <main class="px-4 pt-2">
      <div class="flex flex-col gap-2 mb-2">

        <% body.meals.forEach(function(meal,mIndex) { %>
        <div class="bg-back p-3 rounded-xl">
          <div class="flex items-center justify-between mb-1">
            <span class="font-bold text-base"><%= meal.meal_name %></span>
            <span class="bg-primary px-2 py-0.5 rounded-full text-white flex items-center gap-1.5 text-xs">
              <span><svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" aria-hidden="true" role="img" class="iconify iconify--mdi MuiChip-icon MuiChip-iconMedium MuiChip-iconColorPrimary" font-size="20" width="1em" height="1em" viewBox="0 0 24 24">
                  <path fill="currentColor" d="M12 20a8 8 0 0 0 8-8a8 8 0 0 0-8-8a8 8 0 0 0-8 8a8 8 0 0 0 8 8m0-18a10 10 0 0 1 10 10a10 10 0 0 1-10 10C6.47 22 2 17.5 2 12A10 10 0 0 1 12 2m.5 5v5.25l4.5 2.67l-.75 1.23L11 13V7z"></path>
                </svg></span>
              <span>از <span style="position: relative; top: 1px;"><%= meal.start_time %> تا <%= meal.end_time %></span></span>
              <!-- <span>از <%= meal.start_time %> تا <%= meal.end_time %></span> -->
            </span>
          </div>
          <div class="bg-white rounded-lg overflow-hidden">
            <% meal.menu.forEach(function(menuItem,mtIndex) { %>

            <div class="px-2 py-0.5 border-b-2 border-primary last:border-none">
              <div class="flex items-center gap-2">
                <% if(menuItem.food_image && body.is_pro == false) {%>
                <img class="rounded-md" src="http://storage.morabiha.com<%= menuItem.food_image %>" alt="food" style="width: 31px;">
                <% } %>
                <div>
                  <% if(body.is_pro == false) {%>
                  <p class="font-bold text-xs"><%= menuItem.food_name %></p>
                  <% } %>
                  <p class="text-xs text-gray-600"><%= menuItem.description %></p>
                </div>
              </div>
            </div>

            <% }) %>
          </div>
        </div>

        <% }) %>

      </div>
      <div class="bg-back p-3 rounded-xl">
        <h3 class="mb-1 text-base font-bold">توضیحات کارشناس</h3>
        <p class="text-[11px] text-gray-700 whitespace-pre-line leading-tight">
          <%- body.description?.trim() %>
        </p>
      </div>
    </main>
  </body>




</body>

</html>